name: ami_mobile
description: "Application mobile AMI Sérénité."
publish_to: 'none'
version: 1.0.0+3

environment:
  sdk: '>=3.11.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.9
  http: ^1.6.0
  local_auth: ^3.0.1
  url_launcher: ^6.3.2
  flutter_otp_text_field: ^1.5.1+1
  fluttertoast: ^9.0.0
  intl_phone_number_input: ^0.7.5
  intl: ^0.20.2
  google_fonts: ^8.0.2
  flutter_dash: ^1.0.0
  pdf_widget_wrapper: ^1.0.4
  printing: ^5.14.3
  shared_preferences: ^2.5.5
  animated_splash_screen: ^1.3.0
  page_transition: ^2.2.1
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.14.4
  
 
flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/Favicon-BNA-Assurances.png" # Pour les icônes non adaptatives (par exemple, iOS)
  adaptive_icon_background: '#ffffff' # Image ou couleur d'arrière-plan
  daptive_icon_foreground: "assets/images/Favicon-BNA-Assurances.png" # Image de premier plan


    

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.


# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
   - assets/images/
   - .env
  #   - images/a_dot_ham.jpeg
  

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    - family: HelveticaNeueMedium
      fonts:
        - asset: fonts/HelveticaNeueMedium.otf
    - family: GilmerBold
      fonts:
        - asset: fonts/Gilmer Bold.otf
    - family: GilmerRegular
      fonts:
        - asset: fonts/Gilmer Regular.otf
    - family: GilmerLight
      fonts:
        - asset: fonts/Gilmer Light.otf
    - family: AdventPro
      fonts:
        - asset: fonts/AdventPro-Italic-VariableFont_wdth,wght.ttf
    - family: IBMPlexSansItalic
      fonts:
        - asset: fonts/IBMPlexSans-BoldItalic.ttf
    - family: IBMPlexSans
      fonts:
        - asset: fonts/IBMPlexSans-Bold.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
